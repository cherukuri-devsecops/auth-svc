{{- if .Values.redis.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "auth-app.redis.fullname" . }}
  labels:
    {{- include "auth-app.labels" . | nindent 4 }}
    app.kubernetes.io/component: redis
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.redis.service.port }}
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "auth-app.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: redis
{{- end }}
